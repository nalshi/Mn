# تقرير الإصلاحات الأمنية - نالش (Cloudflare Worker)

تمت مراجعة كل ملفات `src/` (32 ملف) يدوياً وسطراً بسطر، بالإضافة لفحوصات
منهجية (SQL injection، IDOR، تسريب بيانات، secrets، عشوائية ضعيفة). النتائج
والإصلاحات موثّقة أدناه، ومُعلَّقة أيضاً داخل الكود نفسه (ابحث عن `⭐ إصلاح`).

---

## 🔴 حرجة (Critical)

### 1. استيلاء على منتجات تجار آخرين (IDOR) — `productController.js`
جملة `INSERT ... ON CONFLICT(id) DO UPDATE` في `saveProduct` كانت تحلّ
التعارض بالاعتماد على `id` فقط، بدون أي تحقق من أن الصف الموجود مسبقاً يخص
التاجر الحالي. بما أن معرّفات المنتجات ليست سرّية (تظهر بواجهة المتجر
والسلة)، كان أي تاجر يقدر يرسل `id` يخص تاجر آخر ويُغيّر اسمه/سعره/صورته/
توفره مباشرة.
**الإصلاح:** إضافة `WHERE products.merchant_id = excluded.merchant_id`
لجملة `DO UPDATE`، مع التحقق من `result.meta.changes` وإرجاع 403 لو لم يتغير
شيء (أي أن المحاولة كانت على منتج لا يخص التاجر).

### 2. اجتياز مسار (Path Traversal) عبر رفع صورة المنتج
`productId` (نفس `body.id` أعلاه) كان يُستخدم مباشرة ضمن مسار ملف على
GitHub (`images/${username}/${productId}.webp`) بدون أي تحقق من شكله. معرّف
يحتوي `/` أو `..` كان يقدر يحوّل مسار الكتابة الفعلي لأي مكان آخر بمستودع
واجهة المتجر (تخريب متجر آخر بالكامل، أو حقن ملف بمكان حسّاس).
**الإصلاح:** تقييد شكل `id` المرسل من العميل بنمط `^[A-Za-z0-9_-]{1,64}$`
فقط (نفس صيغة المعرّفات المولَّدة تلقائياً)، + تحقق دفاعي إضافي مستقل
(`core/pathSafety.js`) في نقاط بناء المسارات نفسها (`imageService.js`،
`catalogSyncService.js`، `storeInfoSyncService.js`) لحماية `username` أيضاً
كخط دفاع ثانٍ.

---

## 🟠 متوسطة-عالية (Moderate-High)

### 3. تسريب بيانات حسّاسة — `syncController.js` (`get_public_products`)
مسار عام (بدون تسجيل دخول) كان يستخدم `SELECT *`، فيُرجع أيضاً عمود
`cost_price` (تكلفة الشراء) لأي زائر، بينما بقية المشروع (`verifyCartLive`،
كتالوج الواجهة العامة) يتعمّد استثناء هذا العمود تحديداً.
**الإصلاح:** تحديد الأعمدة العامة الآمنة صراحة بالاستعلام.

### 4. تجاوز التحقق من توقيع Webhook واتساب — `whatsappController.js`
لو فعّل تاجر ربط واتساب بدون إدخال `app_secret`، كانت `verifyMetaSignature`
تسمح بأي حدث Webhook وارد بدون توقيع (توثيق مفقود = قبول تلقائي). أي طرف
يعرف `phone_number_id` (غير سرّي) كان يقدر يرسل أحداثاً مزوّرة تستهلك حصة
Gemini الخاصة بالتاجر وتُرسل ردوداً فعلية من رقم واتساب حسابه الحقيقي.
**الإصلاح:** إلزام `app_secret` عند تفعيل الربط (نفس إلزام `access_token`
الموجود مسبقاً)، ورفض الحدث تماماً (fail closed) لو غاب `app_secret`، بدل
قبوله.

### 5. تسريب تفاصيل أخطاء داخلية للعميل — `index.js`
أي خطأ غير متوقع (خطأ قاعدة بيانات، خطأ برمجي..) كان يُرسل `error.message`
الخام مباشرة للعميل، ولم يكن يُسجَّل بالسيرفر إطلاقاً (لا يوجد أي سجلّ حتى
للتشخيص). أخطاء `HttpError` المقصودة (رسائل عربية موجَّهة للمستخدم، وهي
الآلية الأساسية لعرض الأخطاء بكل المشروع) بقيت كما هي بدون أي تغيير.
**الإصلاح:** تسجيل الخطأ الحقيقي بالسيرفر (`console.error`) وإرجاع رسالة
عامة فقط للعميل عند أي خطأ ليس من نوع `HttpError`.

---

## 🟡 أخطاء وظيفية / تحسينات أمنية إضافية

### 6. `syncNewOrderToLegacyApi` معرّفة لكن غير مستدعاة أبداً — `customerController.js`
دالة كاملة بـ `tidbSyncService.js` مخصصة لمزامنة الطلبات الجديدة/المدموجة
مع النظام القديم (api.php / TiDB) — حسب تعليقها التوثيقي بالكود — لم تكن
مستوردة ولا مستدعاة من أي مكان. النتيجة: كل طلب يُنشأ عبر الـ Worker يبقى
غير مرئي تماماً لتطبيقي المندوب والزبون القديمين اللذين يقرآن من TiDB
مباشرة، ولنظام موافقة/رفض الطلب بـ api.php.
**الإصلاح:** ربطها فعلياً عند إنشاء تذكرة جديدة وعند دمج طلب بتذكرة قائمة
(نفس الحالتين اللتين يذكرهما تعليق الدالة نفسه).

### 7. عشوائية غير آمنة تشفيرياً لكود التسليم — `customerController.js`
`Math.random()` لإنشاء كود تأكيد الاستلام (4 أرقام) — قابل للتنبؤ نظرياً.
**الإصلاح:** استبداله بـ `crypto.getRandomValues()`.

### 8. عدم التحقق من قيمة `status` — `orderController.js` (`update_order_status`)
كان يقبل أي نص كقيمة `status` بدون تحقق، رغم أن باقي النظام يفترض قيمة من
`ORDER_STATUSES` المعروفة فقط.
**الإصلاح:** التحقق من القيمة مقابل القائمة المعروفة قبل الحفظ.

### 9. مقارنة غير ثابتة الزمن لمفتاح المزامنة الداخلي — `syncController.js`
التحقق من `X-Internal-Key` كان يستخدم `!==` العادية بدل مقارنة ثابتة الزمن
(بخلاف توقيع واتساب بنفس المشروع الذي يستخدم مقارنة آمنة أصلاً).
**الإصلاح:** إضافة `timingSafeEqual` مشتركة (`security/auth.js`) واستخدامها
بالثلاث نقاط التي تتحقق من هذا المفتاح.

---

## ملاحظات لم تُنفَّذ (توصيات فقط)

- **`confirm_delivery_code`**: لا يوجد حد لعدد محاولات إدخال كود التسليم
  (4 أرقام = 9000 احتمال). التأثير محدود لأن التاجر يستطيع فقط تجربة أكواد
  تذاكره الخاصة، لكن يُستحسن إضافة حد أقصى للمحاولات لاحقاً.
- **معدل الطلبات (`create_order`)**: لا يوجد Rate Limiting على إنشاء
  الطلبات من حساب عميل واحد (بخلاف `ai_chat` الذي له نظام Rate Limiting
  مخصص). يستحق تقييماً منفصلاً حسب حجم الاستخدام الفعلي.

---

## التحقق

جميع الملفات (32 الأصلية + `core/pathSafety.js` الجديد) اجتازت فحص syntax
كامل كوحدات ES Modules (`node --check`) بدون أي خطأ.
